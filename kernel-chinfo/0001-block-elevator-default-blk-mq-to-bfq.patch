From e0a4dd2070ed92999a5902be49e0f88065e8f618 Mon Sep 17 00:00:00 2001
From: Fedora Kernel Team <kernel-team@fedoraproject.org>
Date: Mon, 10 Jan 2022 08:44:35 -0300
Subject: [PATCH] block, elevator: default blk-mq to bfq

blk-mq do not have proper Kconfig selector, so change it here
---
 block/elevator.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/block/elevator.c b/block/elevator.c
index 19a78d5..64fac73 100644
--- a/block/elevator.c
+++ b/block/elevator.c
@@ -628,7 +628,7 @@ static inline bool elv_support_iosched(struct request_queue *q)
 }
 
 /*
- * For single queue devices, default to using mq-deadline. If we have multiple
+ * For single queue devices, default to using bfq. If we have multiple
  * queues or mq-deadline is not available, default to "none".
  */
 static struct elevator_type *elevator_get_default(struct request_queue *q)
@@ -640,7 +640,7 @@ static struct elevator_type *elevator_get_default(struct request_queue *q)
 	    !blk_mq_is_shared_tags(q->tag_set->flags))
 		return NULL;
 
-	return elevator_get(q, "mq-deadline", false);
+	return elevator_get(q, "bfq", false);
 }
 
 /*
-- 
2.34.1

