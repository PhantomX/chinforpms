Firejail SUID
===================

Firejail utilizes Linux namespaces to sandboxing applications, which
require root privileges, so it needs the setuid bit of binary to be set.

To allow users to run firejail, add them to firejail group.

Example:
 usermod -a -G firejail <user>
