diff -up FAudio-19.01/CMakeLists.txt.soversion FAudio-19.01/CMakeLists.txt
--- FAudio-19.01/CMakeLists.txt.soversion	2018-12-31 16:01:51.000000000 -0200
+++ FAudio-19.01/CMakeLists.txt	2019-01-25 09:38:20.886429384 -0200
@@ -152,6 +152,10 @@ else()
 	message(STATUS "no TARGET SDL2::SDL2, or SDL2, using variables")
 	target_include_directories(FAudio PUBLIC $<BUILD_INTERFACE:${SDL2_INCLUDE_DIRS}>)
 	target_link_libraries(FAudio PUBLIC ${SDL2_LIBRARIES})
+	set_target_properties(FAudio PROPERTIES
+		SOVERSION 0
+		VERSION 0.0.0
+	)
 	if(INSTALL_MINGW_DEPENDENCIES)
 		install_shared_libs(bin ${SDL2_LIBRARIES})
 	endif()
